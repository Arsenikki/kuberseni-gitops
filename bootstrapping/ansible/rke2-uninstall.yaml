---
- name: Deploy RKE2
  hosts: all
  become: true
  tasks: 
  - name: Run uninstall RKE2 script 
    ansible.builtin.shell: |
      /usr/local/bin/rke2-uninstall.sh
    args:
      executable: /bin/bash
    