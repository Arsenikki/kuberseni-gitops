---
- name: Deploy RKE2
  hosts: masters
  become: true
  tasks: 
  - name: Run uninstall RKE2 script 
    ansible.builtin.shell: |
      /usr/local/bin/rke2-uninstall.sh
    args:
      executable: /bin/bash
    