---
- name: Deploy RKE2
  hosts: masters
  become: true
  pre_tasks:
  - name: Load secrets
    community.sops.load_vars:
      file: secrets.sops.yaml
      expressions: ignore  # explicitly do not evaluate expressions on load (default)
  roles:
  - role: lablabs.rke2