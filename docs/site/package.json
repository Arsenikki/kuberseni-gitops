{
  "name": "homelab-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "generate": "node scripts/generate.mjs",
    "predev": "npm run sync && npm run generate",
    "dev": "astro dev",
    "prebuild": "npm run sync && npm run generate",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.4",
    "astro": "^7.1.3",
    "astro-mermaid": "^2.1.0",
    "mermaid": "^11.16.0",
    "sharp": "^0.35.3",
    "yaml": "^2.9.0"
  }
}
