crds:
  enabled: true

pspEnable: false

# Run operator on control-plane nodes — workers are resource-constrained
tolerations:
  - key: CriticalAddonsOnly
    operator: Exists
  - key: node-role.kubernetes.io/control-plane
    operator: Exists

nodeSelector:
  node-role.kubernetes.io/control-plane: ""
